namespace src.Enums
{
    public enum TypeState
    {
        //1 = esperando
        wait = 1,
        //2 = enviado
        send = 2,
    }

    public enum TypeShift
    {
        //entrada
        entry,
        //saida
        exit,
    }
}